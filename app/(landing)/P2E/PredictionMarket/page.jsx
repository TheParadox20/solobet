'use client'
import useSWR from "swr";
import { fetcher } from "@/app/lib/data";
import Spinner from "@/app/UI/body/Spinner";

export default function PredictionMarket({params}) {
//   let { data, error, isLoading, mutate } = useSWR(['/games',{path:path.join('/'),search, market}], fetcher);
  return(
    <main>
        <p>Predictions</p>
    </main>
  )
}